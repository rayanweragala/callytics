import{_ as a,o as t,c as i,a2 as n}from"./chunks/framework.DtwafDNl.js";const u=JSON.parse('{"title":"WireGuard VPN","description":"","frontmatter":{},"headers":[],"relativePath":"features/vpn.md","filePath":"features/vpn.md"}'),s={name:"features/vpn.md"};function o(r,e,l,c,d,p){return t(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="wireguard-vpn" tabindex="-1">WireGuard VPN <a class="header-anchor" href="#wireguard-vpn" aria-label="Permalink to &quot;WireGuard VPN&quot;">​</a></h1><p>WireGuard VPN gives remote softphone users a secure private path into Callytics without exposing SIP registration to the public internet. Instead of opening SIP directly to every network, you can require selected extensions to register only through the VPN.</p><p>You can create VPN peers for users, hand them a QR code for quick mobile setup, or provide a downloadable configuration file for desktop clients. The peer list also lets you see who is active and how much traffic each peer has used.</p><p>WireGuard clients are available for iOS, Android, macOS, Windows, and Linux, so the same onboarding flow works for phones, laptops, and desktop softphone setups.</p><h2 id="capabilities" tabindex="-1">Capabilities <a class="header-anchor" href="#capabilities" aria-label="Permalink to &quot;Capabilities&quot;">​</a></h2><ul><li>Secure remote softphone access without publicly exposing SIP registration</li><li>Create VPN peers for individual users or devices</li><li>QR-code onboarding for mobile WireGuard clients</li><li>Downloadable configuration files for desktop clients</li><li>VPN-only restriction for selected SIP extensions</li><li>Peer status monitoring for active and inactive connections</li><li>Data usage visibility per peer</li><li>Client support across iOS, Android, macOS, Windows, and Linux</li></ul><h2 id="external-relay-mode" tabindex="-1">External Relay Mode <a class="header-anchor" href="#external-relay-mode" aria-label="Permalink to &quot;External Relay Mode&quot;">​</a></h2><p>External relay mode is for teams where the Callytics server is behind NAT or has no public IP address. Instead of running WireGuard on the Callytics host, a small public VPS acts as the WireGuard relay server. Remote softphones connect to the VPS, which tunnels traffic back to Callytics over a private WireGuard link.</p><p>This means softphones on mobile data or remote networks can register and make calls without any port forwarding on the Callytics host.</p><h3 id="when-to-use-relay-mode" tabindex="-1">When to use relay mode <a class="header-anchor" href="#when-to-use-relay-mode" aria-label="Permalink to &quot;When to use relay mode&quot;">​</a></h3><ul><li>Your Callytics server is on a LAN with no public IP</li><li>You cannot or do not want to open SIP ports on your router</li><li>You want a dedicated public endpoint for softphone registration separate from your Callytics host</li></ul><h3 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h3><ol><li>Spin up a small VPS (1 CPU, 512MB RAM is enough)</li><li>Open the VPN page in Callytics and click <strong>View Setup Guide</strong> under External Relay</li><li>Follow the nine-step guide — Callytics generates all configuration automatically</li><li>Click <strong>Activate relay tunnel</strong> — no terminal commands needed on the Callytics host</li><li>Configure your softphone to register to the VPS public IP on port 5080</li></ol><p>Once active, the VPN page shows the relay status and softphone connection settings directly on the main card.</p><h3 id="requirements" tabindex="-1">Requirements <a class="header-anchor" href="#requirements" aria-label="Permalink to &quot;Requirements&quot;">​</a></h3><ul><li>A public VPS running Ubuntu or Debian with WireGuard installed</li><li>UDP port 51820 open on the VPS for WireGuard</li><li>UDP port 5080 open on the VPS for SIP</li></ul><h3 id="limitations" tabindex="-1">Limitations <a class="header-anchor" href="#limitations" aria-label="Permalink to &quot;Limitations&quot;">​</a></h3><ul><li>Relay mode and built-in VPN mode cannot run simultaneously</li><li>The relay tunnel does not survive a Callytics restart automatically — reactivate via the UI after restart</li></ul><h2 id="how-relay-mode-works-technically" tabindex="-1">How relay mode works technically <a class="header-anchor" href="#how-relay-mode-works-technically" aria-label="Permalink to &quot;How relay mode works technically&quot;">​</a></h2><h3 id="network-path" tabindex="-1">Network path <a class="header-anchor" href="#network-path" aria-label="Permalink to &quot;Network path&quot;">​</a></h3><p>When relay is active, SIP and RTP traffic follow this path:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Phone (mobile data)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        │ SIP + RTP</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>VPS public IP :5080</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        │ WireGuard tunnel</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>callytics-relay container</span></span>
<span class="line"><span>(Docker bridge 172.20.x.x)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        │ DNAT/SNAT</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>host Asterisk :5080</span></span>
<span class="line"><span>(host network)</span></span></code></pre></div><h3 id="what-callytics-generates-automatically" tabindex="-1">What Callytics generates automatically <a class="header-anchor" href="#what-callytics-generates-automatically" aria-label="Permalink to &quot;What Callytics generates automatically&quot;">​</a></h3><p>When you activate relay mode, Callytics generates two PJSIP configuration files and reloads them into Asterisk via AMI:</p><p><strong><code>pjsip_relay.conf</code></strong> — sets the transport-level NAT overrides so Asterisk advertises the correct address in SIP headers:</p><ul><li><code>external_signaling_address = &lt;VPS public IP&gt;</code> — used in the <code>Contact</code> and <code>Via</code> headers of outgoing SIP messages</li><li><code>external_media_address = &lt;VPS public IP&gt;</code> — used in the <code>c=</code> line of SDP, telling the remote party where to send RTP</li></ul><p><strong><code>pjsip_extensions_relay.conf</code></strong> — adds per-endpoint RTP overrides to every SIP extension endpoint:</p><ul><li><code>media_address = &lt;VPS public IP&gt;</code> — forces RTP to advertise the VPS IP for this endpoint</li><li><code>rtp_symmetric = yes</code> — sends RTP to wherever packets arrive from, handling any NAT port remapping mid-call</li></ul><h3 id="why-10-8-0-0-24-must-not-be-in-local-net" tabindex="-1">Why <code>10.8.0.0/24</code> must NOT be in <code>local_net</code> <a class="header-anchor" href="#why-10-8-0-0-24-must-not-be-in-local-net" aria-label="Permalink to &quot;Why \`10.8.0.0/24\` must NOT be in \`local_net\`&quot;">​</a></h3><p>Asterisk&#39;s <code>local_net</code> setting tells it which addresses to treat as local (and therefore not requiring NAT rewriting). The WireGuard subnet <code>10.8.0.0/24</code> must not be in <code>local_net</code>.</p><p>If it is, Asterisk sees the WireGuard tunnel peer <code>10.8.0.1</code> as a local address. It then uses the Docker bridge IP in the <code>Contact</code> header instead of the VPS public IP, which breaks call setup — the remote party receives an unreachable address for SIP signaling.</p><h3 id="why-mixing-dtmf-events-bridge-type-is-required" tabindex="-1">Why <code>mixing,dtmf_events</code> bridge type is required <a class="header-anchor" href="#why-mixing-dtmf-events-bridge-type-is-required" aria-label="Permalink to &quot;Why \`mixing,dtmf_events\` bridge type is required&quot;">​</a></h3><p>When Stasis creates a bridge to connect two call parties, it always uses bridge type <code>mixing,dtmf_events</code>. This is required for NAT relay scenarios.</p><p>If a plain <code>mixing</code> bridge is used, Asterisk can upgrade it to a native RTP bridge after call answer. In a native RTP bridge, Asterisk steps out of the RTP path entirely — the two endpoints exchange RTP directly. Through a NAT relay, this breaks audio after approximately 30 seconds when NAT bindings expire or change.</p><p>Using <code>mixing,dtmf_events</code> prevents the native bridge upgrade and keeps Asterisk in the media path throughout the call.</p><h3 id="strictrtp-no-in-rtp-conf" tabindex="-1"><code>strictrtp=no</code> in <code>rtp.conf</code> <a class="header-anchor" href="#strictrtp-no-in-rtp-conf" aria-label="Permalink to &quot;\`strictrtp=no\` in \`rtp.conf\`&quot;">​</a></h3><p>Callytics always sets <code>strictrtp=no</code> in Asterisk&#39;s <code>rtp.conf</code>. This prevents RTP packet drops when the source address of incoming RTP changes mid-call — a common occurrence when calls are routed through NAT. Without this setting, Asterisk may reject RTP packets from a new source port after a NAT binding changes.</p>`,37)])])}const m=a(s,[["render",o]]);export{u as __pageData,m as default};
